<!DOCTYPE html>
<html>
  <head>
    <title>MySNSApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="margin: 0; padding: 0; background-color: #f5f8fa;">
    <header style="background-color: #ffffff; border-bottom: 1px solid #ddd; padding: 10px 20px; position: sticky; top: 0; z-index: 1000;">
      <div style="max-width: 1000px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center;">
        
        <div>
          <%= link_to "My SNS", root_path, style: "font-size: 1.5rem; font-weight: bold; text-decoration: none; color: #007bff;" %>
        </div>

        <nav style="display: flex; align-items: center; gap: 20px;">
          <% if user_signed_in? %>
            <%= link_to "メッセージ", rooms_index_path, style: "text-decoration: none; color: #333; font-weight: 500;" %>

            <%= link_to notifications_path, style: "text-decoration: none; color: #333; font-weight: 500; position: relative;" do %>
              通知
              <% if current_user.passive_notifications.where(checked: false).any? %>
                <span style="background-color: #ff4d4d; color: white; border-radius: 50%; padding: 2px 6px; font-size: 0.7rem; position: absolute; top: -10px; right: -15px;">
                  <%= current_user.passive_notifications.where(checked: false).count %>
                </span>
              <% end %>
            <% end %>

            <%= link_to "プロフィール", user_path(current_user), style: "text-decoration: none; color: #333; font-weight: 500;" %>
            
            <%= link_to "ログアウト", logout_path, style: "text-decoration: none; color: #ff4d4d; font-weight: bold;" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, style: "text-decoration: none; color: #007bff;" %>
            <%= link_to "新規登録", new_user_registration_path, style: "text-decoration: none; color: #333;" %>
          <% end %>
        </nav>
      </div>
    </header>

    <% if notice %>
      <div style="background-color: #d4edda; color: #155724; padding: 10px; text-align: center; border-bottom: 1px solid #c3e6cb;">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div style="background-color: #f8d7da; color: #721c24; padding: 10px; text-align: center; border-bottom: 1px solid #f5c6cb;">
        <%= alert %>
      </div>
    <% end %>

    <main style="padding-top: 20px;">
      <%= yield %>
    </main>

  </body>
</html>