<div style="max-width: 600px; margin: 2rem auto; padding: 2rem; border: 1px solid #ddd; border-radius: 8px; font-family: sans-serif;">
  <h2 style="text-align: center; margin-bottom: 2rem;">投稿を編集</h2>

  <%= form_with model: @post, local: true do |f| %>
    <div style="margin-bottom: 1.5rem;">
      <%= f.label :content, "内容", style: "font-weight: bold;" %><br>
      <%= f.text_area :content, style: "width: 100%; height: 120px; padding: 10px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; margin-top: 10px; font-size: 1rem;" %>
    </div>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 2rem;">
      <div style="display: flex; align-items: center; gap: 15px;">
        <%= f.submit "変更を保存する", style: "background-color: #007bff; color: white; border: none; padding: 10px 20px; border-radius: 4px; cursor: pointer; font-weight: bold;" %>
        <%= link_to "キャンセル", root_path, style: "color: #666; text-decoration: none; font-size: 0.9rem;" %>
      </div>

      <%# 究極の回避策：リンク移動として削除を実行する %>
      <%= link_to "この投稿を削除する", post_path(@post, _method: :delete), 
                  data: { turbo: false }, 
                  onclick: "return confirm('本当に削除しますか？')", 
                  style: "color: #ff4d4d; font-size: 0.85rem; text-decoration: underline; cursor: pointer;" %>
    </div>
  <% end %>
</div>