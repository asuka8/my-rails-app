<div style="max-width: 600px; margin: 0 auto; background: white; min-height: 100vh; display: flex; flex-direction: column; border-left: 1px solid var(--border-color); border-right: 1px solid var(--border-color); position: relative;">
  
  <div style="position: sticky; top: 60px; background: rgba(255,255,255,0.9); backdrop-filter: blur(10px); z-index: 100; padding: 10px 20px; border-bottom: 1px solid var(--border-color); display: flex; align-items: center; justify-content: space-between;">
    <div style="display: flex; align-items: center; gap: 12px;">
      <%# 相手の情報を取得 %>
      <% opponent = @entries.where.not(user_id: current_user.id).first.user %>
      <%= link_to user_path(opponent) do %>
        <% if opponent.profile_image.attached? %>
          <%= image_tag opponent.profile_image, style: "width: 40px; height: 40px; border-radius: 50%; object-fit: cover;" %>
        <% else %>
          <div style="width: 40px; height: 40px; border-radius: 50%; background: #eee; display: flex; align-items: center; justify-content: center;">👤</div>
        <% end %>
      <% end %>
      <span style="font-weight: bold;"><%= opponent.nickname.presence || opponent.email %></span>
    </div>

    <%# --- 修正箇所：戻るボタン --- %>
    <%= link_to "終了", rooms_index_path, style: "text-decoration: none; color: var(--text-sub); font-size: 0.9rem; font-weight: bold; padding: 6px 16px; border-radius: 99px; border: 1px solid var(--border-color); background: white;" %>
  </div>

  <div style="flex: 1; padding: 20px; display: flex; flex-direction: column; gap: 15px;">
    <% if @messages.any? %>
      <% @messages.each do |m| %>
        <% is_mine = (m.user_id == current_user.id) %>
        <div style="display: flex; justify-content: <%= is_mine ? 'flex-end' : 'flex-start' %>; margin-bottom: 10px;">
          <div style="max-width: 70%;">
            <div style="background: <%= is_mine ? '#1d9bf0' : '#eff3f4' %>; color: <%= is_mine ? 'white' : 'black' %>; padding: 10px 15px; border-radius: 18px;">
              <%= m.content %>
            </div>
            
            <%# 自分の投稿かつ既読（checked: true）なら「既読」と表示 %>
            <% if is_mine && m.checked %>
              <div style="font-size: 0.7rem; color: #666; text-align: right; margin-top: 2px;">既読</div>
            <% end %>
            
            <div style="font-size: 0.7rem; color: #666; text-align: <%= is_mine ? 'right' : 'left' %>;">
              <%= m.created_at.strftime("%H:%M") %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <p style="text-align: center; color: var(--text-sub); margin-top: 50px;">メッセージを送ってみましょう！</p>
    <% end %>
  </div>

  <div style="position: sticky; bottom: 0; background: white; padding: 15px 20px; border-top: 1px solid var(--border-color);">
    <%# 修正点：model: @message に変更 %>
    <%= form_with model: @message, local: true, style: "display: flex; gap: 10px; align-items: center;" do |f| %>
      <%= f.text_field :content, placeholder: "メッセージを作成", style: "flex: 1; padding: 10px 15px; border-radius: 99px; border: 1px solid var(--border-color); background: #f7f9f9; outline: none; font-size: 0.95rem;" %>
      
      <%# RoomのIDを渡すことで、どの部屋のメッセージかをコントローラーに伝えます %>
      <%= f.hidden_field :room_id, value: @room.id %>
      
      <%= f.submit "送信", style: "background: var(--primary-color); color: white; border: none; padding: 8px 18px; border-radius: 99px; font-weight: bold; cursor: pointer;" %>
    <% end %>
  </div>
</div>