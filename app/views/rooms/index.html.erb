<div style="max-width: 600px; margin: 0 auto; background: white; min-height: 100vh; border-left: 1px solid #eee; border-right: 1px solid #eee;">
  
  <div style="padding: 15px 20px; border-bottom: 1px solid #eee; position: sticky; top: 0; background: rgba(255,255,255,0.9); backdrop-filter: blur(10px); z-index: 100;">
    <h2 style="margin: 0; font-size: 1.2rem; font-weight: bold;">メッセージ</h2>
  </div>

  <% if @another_entries.present? %>
    <% @another_entries.each do |e| %>
      <%# 最新のメッセージを取得 %>
      <% last_message = e.room.messages.order(created_at: :desc).first %>
      
      <%= link_to room_path(e.room.id), style: "text-decoration: none; color: inherit;" do %>
        <div style="padding: 15px 20px; border-bottom: 1px solid #eee; display: flex; align-items: center; gap: 12px; transition: background 0.2s;" onmouseover="this.style.background='#f7f9f9'" onmouseout="this.style.background='transparent'">
          
          <%# 相手のアイコン %>
          <% if e.user.profile_image.attached? %>
            <%= image_tag e.user.profile_image, style: "width: 48px; height: 48px; border-radius: 50%; object-fit: cover;" %>
          <% else %>
            <div style="width: 48px; height: 48px; border-radius: 50%; background: #eee; display: flex; align-items: center; justify-content: center;">👤</div>
          <% end %>
          
          <div style="flex: 1;">
            <div style="display: flex; justify-content: space-between; align-items: baseline;">
              <span style="font-weight: bold;"><%= e.user.nickname.presence || e.user.email %></span>
              <span style="font-size: 0.8rem; color: #666;">
                <%= last_message&.created_at&.strftime("%m月%d日") %>
              </span>
            </div>
            
            <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 4px;">
              <p style="margin: 0; color: #666; font-size: 0.9rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 350px;">
                <%= last_message&.content || "メッセージはありません" %>
              </p>
              
              <%# 未読がある場合に青いドットを表示 %>
              <% unread_count = e.room.messages.where.not(user_id: current_user.id).where(checked: false).count %>
              <% if unread_count > 0 %>
                <div style="background: #1d9bf0; color: white; font-size: 0.7rem; padding: 2px 6px; border-radius: 10px; font-weight: bold;">
                  <%= unread_count %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  <% else %>
    <div style="padding: 100px 20px; text-align: center; color: #666;">
      <p style="font-weight: bold; font-size: 1.2rem; color: #000;">新しいメッセージを届けよう</p>
      <p>会話を始めると、ここに表示されます。</p>
    </div>
  <% end %>
</div>